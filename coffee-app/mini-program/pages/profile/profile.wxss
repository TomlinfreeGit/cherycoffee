/* filepath: coffee-app/mini-program/pages/profile/profile.wxss */
.page-profile {
  padding: 24rpx;
  padding-bottom: 80rpx;
}

.loading {
  text-align: center;
  color: var(--muted);
  padding: 80rpx 0;
}

/* ─── 会员等级卡片 ──────────────────────────────────── */
.level-card {
  background: linear-gradient(135deg, #ff9966 0%, #ff5e62 100%);
  color: #fff;
  border-radius: 16rpx;
  padding: 32rpx 28rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 4rpx 16rpx rgba(255, 94, 98, 0.25);
}
.level-card-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 24rpx;
}
.level-badge {
  background: rgba(255, 255, 255, 0.25);
  padding: 8rpx 24rpx;
  border-radius: 100rpx;
  font-size: 28rpx;
  font-weight: 700;
  letter-spacing: 1rpx;
}
.level-discount {
  font-size: 36rpx;
  font-weight: 700;
}
.level-progress-label {
  display: flex;
  justify-content: space-between;
  font-size: 22rpx;
  opacity: 0.95;
  margin-bottom: 12rpx;
}
.level-progress-track {
  height: 12rpx;
  background: rgba(255, 255, 255, 0.3);
  border-radius: 100rpx;
  overflow: hidden;
}
.level-progress-fill {
  height: 100%;
  background: #fff;
  border-radius: 100rpx;
  transition: width 0.3s ease;
}
.level-progress-foot {
  display: flex;
  justify-content: space-between;
  font-size: 20rpx;
  opacity: 0.85;
  margin-top: 8rpx;
}

.section {
  background: var(--panel);
  border-radius: 16rpx;
  padding: 32rpx 28rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
}

.section-title {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text);
  margin-bottom: 24rpx;
}

.hint {
  font-size: 22rpx;
  color: var(--muted);
  flex: 1;
  margin-top: 12rpx;
}

.nickname-row {
  background: var(--bg);
  border-radius: 12rpx;
  padding: 0 20rpx;
}

.nickname-input {
  height: 80rpx;
  font-size: 28rpx;
  color: var(--text);
}

.phone-display {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: var(--bg);
  border-radius: 12rpx;
  padding: 24rpx 28rpx;
  margin-bottom: 16rpx;
}

.phone-masked {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text);
  letter-spacing: 1rpx;
}

.phone-status {
  font-size: 22rpx;
  color: #16a34a;
  background: rgba(22, 163, 74, 0.1);
  padding: 4rpx 12rpx;
  border-radius: 999rpx;
}

.phone-empty {
  display: block;
  text-align: center;
  padding: 24rpx 0;
  margin-bottom: 16rpx;
}

.phone-btn {
  width: 100%;
  height: 88rpx;
  font-size: 30rpx;
  border-radius: 999rpx;
}

.btn {
  height: 80rpx;
  font-size: 28rpx;
  border-radius: 999rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.btn-primary {
  background: var(--primary);
  color: #fff;
}

.btn-secondary {
  background: var(--bg);
  color: var(--text);
  border: 2rpx solid var(--border);
}

.manual-toggle {
  text-align: center;
  font-size: 24rpx;
  color: var(--muted);
  padding: 24rpx 0 8rpx;
}

.manual-toggle:active {
  opacity: 0.6;
}

.manual-row {
  display: flex;
  gap: 16rpx;
  margin-top: 16rpx;
}

.manual-input {
  flex: 1;
  height: 80rpx;
  background: var(--bg);
  border-radius: 12rpx;
  padding: 0 20rpx;
  font-size: 28rpx;
  color: var(--text);
}

.manual-save {
  width: 160rpx;
  height: 80rpx;
  font-size: 28rpx;
}

.manual-save[disabled] {
  opacity: 0.5;
}

.manual-hint {
  margin-top: 8rpx;
  text-align: center;
}

.footer-hint {
  margin-top: 40rpx;
  padding: 24rpx 32rpx;
  font-size: 22rpx;
  color: var(--muted);
  line-height: 1.8;
  text-align: center;
}
