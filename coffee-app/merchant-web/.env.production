# filepath: coffee-app/merchant-web/.env.production
# Used by `npm run build`. After build, the dist bundle makes requests
# directly to these absolute URLs (no Vite proxy at runtime).
#
# Override at build time with:
#   VITE_API_BASE=https://other.host/api npm run build

VITE_API_BASE=
VITE_UPLOADS_BASE=
